package com.game.extravirgin.main;

public enum ID {
	//Do not use this class anymore.
	//Instead of checking the ID, use the java statement if(object instanceof Player) 
	//to determine what type of entity you are dealing with then cast: ((Player) object).doStuff();
	
	//will use this as reference, do not delete
}
